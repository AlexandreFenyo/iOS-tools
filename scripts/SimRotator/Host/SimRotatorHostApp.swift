//
//  SimRotatorHostApp.swift — app hôte vide, exigée par Xcode pour une cible de tests
//  d'interface ; le test ne la lance jamais (il ne fait que pivoter l'appareil).
//
import SwiftUI

@main
struct SimRotatorHostApp: App {
    var body: some Scene {
        WindowGroup { Text("SimRotator") }
    }
}
